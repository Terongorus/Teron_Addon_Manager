<Window x:Class="Teron_Addon_Manager.AddAddonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Addon"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Addon page or download URL" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" x:Name="urlTextBox" Height="32" VerticalContentAlignment="Center" Padding="8,0"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="okButton" Content="OK" Width="90" Height="32" IsDefault="True" Margin="0,0,8,0"/>
            <Button x:Name="cancelButton" Content="Cancel" Width="90" Height="32" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
